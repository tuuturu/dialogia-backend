version: "3.7"

services:
  backend:
    image: docker.pkg.github.com/tuuturu/dialogia-backend/dialogia-backend:0.0.1
    build: .
    ports:
      - '3000:3000'
      - '8081:8081'
    networks:
      - dialogia

networks:
  dialogia:
